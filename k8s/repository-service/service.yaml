apiVersion: v1
kind: Service
metadata:
  name: repository-service
  namespace: code-review-platform
spec:
  selector:
    app: repository-service
  ports:
    - port: 8000
      targetPort: 8000
      name: http
