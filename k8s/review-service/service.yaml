apiVersion: v1
kind: Service
metadata:
  name: review-service
  namespace: code-review-platform
spec:
  selector:
    app: review-service
  ports:
    - port: 8000
      targetPort: 8000
      name: http
