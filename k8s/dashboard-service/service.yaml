apiVersion: v1
kind: Service
metadata:
  name: dashboard-service
  namespace: code-review-platform
spec:
  selector:
    app: dashboard-service
  ports:
    - port: 3000
      targetPort: 3000
      name: http
